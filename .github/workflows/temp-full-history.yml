name: TEMP Full Holiday History

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/temp-full-history.yml

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Read diagnostic
        run: |
          set -e
          sleep 15
          curl -sS --fail-with-body --max-time 40 https://centraladapta.febredearte.workers.dev/__diag-hist-7f3b9c2a
